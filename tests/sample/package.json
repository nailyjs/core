{
  "name": "sample",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "npm run build && node .naily/cjs/main",
    "build": "rollup -c rollup.config.ts --configPlugin @rollup/plugin-typescript"
  },
  "dependencies": {
    "@nailyjs/core": "workspace:*"
  }
}
