{
  "name": "sample",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run build && node .naily/cjs/main",
    "build": "naily build app"
  },
  "dependencies": {
    "@nailyjs/core": "workspace:*",
    "@nailyjs/cli": "workspace:*"
  }
}
